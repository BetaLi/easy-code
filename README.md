sudo apt update && sudo apt install -y nodejs=12.*

apt install npm 

npm install pm2 -g

sudo apt-get update && sudo apt-get install -y git

ssh-keygen -t rsa -b 4096 -C "linelijun@163.com"

cat /root/.ssh/id_rsa.pub

pm2 serve ./build/ 80
